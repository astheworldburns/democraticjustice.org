{
  "name": "admin-gateway",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "seed": "wrangler dev ./scripts/seed-user.js --config wrangler.toml"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
